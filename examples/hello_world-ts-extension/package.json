{
  "name": "hello-world-ts-extension",
  "version": "1.0.0",
  "description": "TypeScript example extension that broadcasts Hello World messages over UDP",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "build:check": "tsc --noEmit",
    "clean": "rm -rf dist install node_modules",
    "package-lvm": "npm run build && bash ../common-scripts/pkg-dev.sh install lvm hello_world",
    "package-ubi": "npm run build && bash ../common-scripts/pkg-dev.sh install ubi hello_world"
  },
  "author": "BrightSign LLC",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
